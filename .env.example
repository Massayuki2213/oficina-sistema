# ==========================================================
# Hermes — variáveis de ambiente (copie para .env e ajuste)
# ==========================================================

# ---- Banco de dados (PostgreSQL) ----
# Local (docker-compose). Em produção, troque pela URL da nuvem (Supabase/Neon/Railway).
# Se a máquina já tiver outro Postgres na 5432, mude POSTGRES_PORT (ex: 5433) e
# ajuste a porta na DATABASE_URL — o docker-compose usa essa variável.
POSTGRES_PORT=5432
DATABASE_URL="postgresql://hermes:hermes_dev@localhost:5432/hermes?schema=public"

# ---- Cache (Redis) ----
REDIS_PORT=6379
REDIS_URL="redis://localhost:6379"

# ---- API ----
API_PORT=3333
API_HOST=0.0.0.0
NODE_ENV=development

# ---- Segurança / Login (JWT + perfis) ----
JWT_SECRET="troque-por-uma-chave-longa-e-secreta"
JWT_EXPIRES_IN="8h"

# ---- Backup automático do banco ----
# Roda quando a cópia mais recente passa de 24h (inclusive ao ligar o PC).
BACKUP_ENABLED=true
BACKUP_DIR="./backups"
BACKUP_RETENCAO_DIAS=14
BACKUP_CONTAINER="hermes-postgres"
